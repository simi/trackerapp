
= form_for [:admin, @user], html: { id: 'admin-user-form', class: 'form-horizontal'} do |f|

  = render 'layouts/errors', object: @user

  .form-group
    = f.label :username, t('admin.username'), class: 'col-sm-4 control-label'
    .col-sm-4
      = f.text_field :username, class: 'form-control'

  .form-group
    = f.label :email, t('admin.email'), class: 'col-sm-4 control-label'
    .col-sm-4
      = f.text_field :email, class: 'form-control'

  .form-group
    = f.label :password, t('admin.password'), class: 'col-sm-4 control-label'
    .col-sm-4
      = f.password_field :password, class: 'form-control'

  .form-group
    = f.label :password_confirmation, t('admin.password_confirmation'), class: 'col-sm-4 control-label'
    .col-sm-4
      = f.password_field :password_confirmation, class: 'form-control'

  .form-group
    = f.label :language, t('admin.language'), class: 'col-sm-4 control-label'
    .col-sm-4
      = f.text_field :language, class: 'form-control'

  .form-group
    .col-sm-offset-4.col-sm-4
      .checkbox
        label
          = f.check_box :admin
          | #{t('admin.admin')}?

  .form-group
    .col-sm-offset-4.col-sm-4
      = f.submit t('admin.save'), class: 'btn btn-default'
