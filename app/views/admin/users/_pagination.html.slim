div class="box-nav pagination-#{position}"

  = link_to admin_user_path(@user, from: @previous_month.to_s(:db)), class: 'btn-box' do
    = l(@previous_month, :format => "%B")

  span.btn-box = l(@from, :format => "%B")

  = link_to admin_user_path(@user, from: @next_month.to_s(:db)), class: 'btn-box' do
    = l(@next_month, :format => "%B")
